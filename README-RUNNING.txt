## How to Run the Project (Fixed by BLACKBOXAI)

**Frontend (Vite/React):**
1. `npm install`
2. `npm start` (runs on http://localhost:3000)

**Fixes Applied:**
- PowerShell execution policy for npm
- Vite server startup
- Project now viewable immediately

**Backend:** Run `cd backend && npm install && npm start` (if backend/package.json exists)

Empty commit created to document these fixes: c5bc89c
